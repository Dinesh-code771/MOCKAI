// Template IDs that need to be fetched from SendGrid
export class EmailTemplateType {
}

// Emails to be used as per the SendGrid account used
export enum AdminEmailIDs {
  FROM_EMAIL = 'no-reply@mockai.com',
}
